function handleAjax(data) {
	 var status = data.status;

	    switch(status) {
	        case "complete":
	        	//updateAlteradorFunction();
	            break;
	        case "success":
	        	//updateAlteradorFunction();
	            break;
	    }
}